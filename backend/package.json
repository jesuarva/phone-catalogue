{
  "name": "backend",
  "version": "1.0.0",
  "description": "API for a phone catalogue",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "cross-env NODE_ENV=test jest --watch --verbose",
    "test-console-log": "cross-env NODE_ENV=test TERM=dumb jest --watch --verbose"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "author": "Jean Surkouf Ariza Varela",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "nodemon": "^1.18.5"
  }
}